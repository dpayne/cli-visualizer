- move spectrum stuff into one visualizer
    - three different smoothing modes sgs, monster, none
    - add fall off effect
        - make it configurable
- make things more configurable
    - monster cat smoothing params
    - auto sensing sensitivity
- fix shutdown so that when mpd stops ctrl-C works, when mpd is stopped it should just wait for input
- make ellipse
- make simple wave matching ncmcpcpp
- make circle transformer possibly make it rotate
- make color schemes instead of a file
- figure out better color spectrum
- read from alsa
- use make_unique instead
- get rid of direct member variable access in Spectrum transformer and children
- writing more unit tests
