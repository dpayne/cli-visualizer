- read from alsa
- fix buffer reader so that it reads into an array or something where the allocation isn't with malloc
- writing more unit tests
- make circle transformer possibly make it rotate
- make spectrum's top char fall slowly
- figure out font to match cava
- figure out better color spectrum
- make color schemes instead of a file
- restore colors
