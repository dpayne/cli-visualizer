language: cpp

compiler:
    - clang++
    - g++
install:
- if [ "$CXX" = "g++" ]; then export CXX="g++-4.9" CC="gcc-4.9"; fi

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - libfftw3-dev
    - libncursesw5-dev
    - gcc-4.9
    - g++-4.9
    - clang
